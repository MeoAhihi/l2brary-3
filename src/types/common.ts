/* eslint-disable no-unused-vars */

export enum PAGE_NAME {
  LOGIN = "login",
  SIGN_UP = "signUp",
  DASHBOARD = "dashboard",
  BIRTHDAY = "birthday",
  MEMBERS = "members",
  PROFILE = "profile",
}

export type LabelValue = {
  label: string;
  value: string;
};
